import { Link } from "gatsby";

import Layout from "src/components/layout";
import Wrapper from "src/components/design-system/wrapper";
import Header from "src/components/design-system/header";
import ImageSource from "src/components/image";
import UsageGuidelines from "src/components/design-system/usage_guidelines";
import Counter from "src/components/counter";

export default props => {
  const { children } = props;
  return (
    <Layout>
      <Header title="Dropdown menus" stability="Alpha" />
      <Wrapper>{children}</Wrapper>
    </Layout>
  );
};

A dropdown menu presents a group of selections and allows users to select one of the items. Display of the list is activated by user clicking an arrow icon or text area.

<ImageSource src="dropdowns/dropdown.png" alt="Dropdown menu" />

# Usage guideline

<UsageGuidelines
  dos={[
    `Default text indicates expected user’s action.`,
    `Default text and label clearly shows what the list items are grouped for.`,
    `List item text is visually dominent than default text.`,
    `Use sentence case.`
  ]}
  donts={[
    `Inconsistent language.`,
    `The item wording is lenghy that it prevents for users from fast scanning.`,
    `Use a sentence as a list item.`,
    `Note: If being descriptive is required, consider using a checkbox or radio button approach.`
  ]}
/>

# Interactive states

<ImageSource src="dropdowns/interactive_states.png" alt="Interactive states" />

<br />






```js live=true

<Counter start={100} />


```


```js

class Counter extends React.Component {
  constructor() {
    super()
    this.state = { count: 0 }
  }

  componentDidMount() {
    this.interval = setInterval(() => {
      this.setState(state => ({ count: state.count + 1 }))
    }, 1000)
  }

  componentWillUnmount() {
    clearInterval(this.interval)
  }

  render() {
    return (
      <center>
        <h3>
          {this.state.count}
        </h3>
      </center>
    )
  }
}

```
